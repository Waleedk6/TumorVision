# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/tumorvision
spring.data.mongodb.database=tumorvision

# JWT Secret Key (change this to something more secure later)
jwt.secret=tumorVisionSecretKey
jwt.expirationMs=86400000 # 24 hours

# Temporary admin user (we'll remove this later)
spring.security.user.name=admin
spring.security.user.password=admin